### Why .git Folder is Important

The .git folder is the heart and brain of a Git repository
If you delete it, your project is no longer a Git repository.

Stores Complete History

Every commit you ever made

Author, timestamp, message

Previous versions of files

📌 That’s why Git can rollback anytime

| Component     | Purpose                   |
| ------------- | ------------------------- |
| `HEAD`        | Points to current branch  |
| `config`      | Repo-level configuration  |
| `objects/`    | All commits, trees, blobs |
| `refs/`       | Branches & tags           |
| `logs/`       | History of branch changes |
| `index`       | Staging area              |
| `hooks/`      | Git hooks                 |
| `packed-refs` | Optimized refs storage    |

